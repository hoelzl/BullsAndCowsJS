import React from 'react';
import { shallow, mount } from 'enzyme';
import { Bull, Cow, Dud } from './Styles';

import GuessEvaluationSegment from './GuessEvaluationSegment';

describe('<GuessEvaluationSegment />', () => {
  it('misses all the tests', () => {});
});
